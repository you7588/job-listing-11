<div class="col-md-10 job-box">
  <div class="col-md-6">
    <h3><%= link_to(job.title, job_path(job)) %></h3>
    <span><%= job.created_at %></span>
    <p><%= job.wage_lower_bound %> ~ <%= job.wage_upper_bound %></p>
  </div>
  <div class="col-md-4">
    <h3><%= job.source %></h3>
    <p><%= job.place %></p>
    <p><%= job.scale %></p>
  </div>
</div>
